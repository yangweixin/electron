{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . electron --platform=mas --arch=x64 --electron-version=2.0.5 --overwrite --ignore=node_modules --ignore=.gitignore"
  },
  "author": "young",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.5"
  }
}
